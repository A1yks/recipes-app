import { useRouter } from 'next/router';
import { useGetRecipeQuery } from 'src/services/api';

function useEditRecipe() {
    // const router = useRouter();
    // const {} = useGetRecipeQuery();
}

export default useEditRecipe;
